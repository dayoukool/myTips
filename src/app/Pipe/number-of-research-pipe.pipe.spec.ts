import { NumberOfResearchPipePipe } from './number-of-research-pipe.pipe';

describe('NumberOfResearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfResearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
