import { FilterDatePipePipe } from './filter-date-pipe.pipe';

describe('FilterDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
