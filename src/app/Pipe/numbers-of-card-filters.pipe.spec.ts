import { NumbersOfCardFiltersPipe } from './numbers-of-card-filters.pipe';

describe('NumbersOfCardFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersOfCardFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
