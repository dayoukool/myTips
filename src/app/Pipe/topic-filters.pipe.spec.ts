import { TopicFiltersPipe } from './topic-filters.pipe';

describe('TopicFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
